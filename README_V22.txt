# V22 Print Image Center

تشغيل:
pip install -r requirements.txt
streamlit run app_V22_Print_Image_Center.py

الجديد:
- حل مشكلة الطباعة الفارغة
- الطباعة تعتمد على صورة PNG مولّدة من PDF الحقيقي
- استمرار PDF + Word + Bulk + ZIP + PDF موحد

مهم:
ثبّت pymupdf:
pip install pymupdf
